/**
 * Shared TypeScript interfaces for the Pi generator.
 *
 * TODO: Define CommandYaml interface (command YAML input shape)
 * TODO: Define AgentYaml interface (agent YAML input shape)
 * TODO: Define GeneratorOptions interface (--dry-run, --verbose, --validate, --force)
 * TODO: Define TransformResult interface (generated content + metadata)
 *
 * @module ensemble-pi/types
 */

// TODO: implement
export {};
