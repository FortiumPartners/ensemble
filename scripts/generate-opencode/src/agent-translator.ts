/**
 * AgentTranslator - Converts YAML agents to OpenCode JSON config and Markdown
 *
 * Responsibilities:
 *   - Parse Ensemble agent YAML schema
 *   - Map tool permissions to OpenCode format
 *   - Concatenate mission/responsibilities into OpenCode prompt field
 *   - Classify agent modes (orchestrator -> primary, others -> subagent)
 *   - Translate model hints to OpenCode providerID/modelID format
 *   - Generate OpenCode agent JSON and Markdown files
 *
 * Task IDs: OC-S2-AGT-001 through OC-S2-AGT-011
 */

// Stub: Implementation will be added in OC-S2-AGT-001
export class AgentTranslator {
  // TODO: Implement in OC-S2-AGT-001
}
